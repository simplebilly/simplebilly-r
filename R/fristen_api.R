#' SimpleBilly API
#'
#' Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
#'
#' The version of the OpenAPI document: 0.1.0
#' Contact: support@simplebilly.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Fristen operations
#' @description FristenApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  FristenApi  ####################
#'
#' library(openapi)
#' var_bundesland <- "bundesland_example" # character |  (Optional)
#' var_voranmeldungsrhythmus <- "voranmeldungsrhythmus_example" # character |  (Optional)
#' var_dauerfristverlaengerung <- "dauerfristverlaengerung_example" # character |  (Optional)
#' var_est_aktiv <- "est_aktiv_example" # character |  (Optional)
#' var_gewst_aktiv <- "gewst_aktiv_example" # character |  (Optional)
#' var_monate <- 56 # integer |  (Optional)
#'
#' api_instance <- FristenApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$FristenApi(bundesland = var_bundesland, voranmeldungsrhythmus = var_voranmeldungsrhythmus, dauerfristverlaengerung = var_dauerfristverlaengerung, est_aktiv = var_est_aktiv, gewst_aktiv = var_gewst_aktiv, monate = var_monatedata_file = "result.txt")
#' result <- api_instance$FristenApi(bundesland = var_bundesland, voranmeldungsrhythmus = var_voranmeldungsrhythmus, dauerfristverlaengerung = var_dauerfristverlaengerung, est_aktiv = var_est_aktiv, gewst_aktiv = var_gewst_aktiv, monate = var_monate)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FristenApi <- R6::R6Class(
  "FristenApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new FristenApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param bundesland (optional) No description
    #' @param voranmeldungsrhythmus (optional) No description
    #' @param dauerfristverlaengerung (optional) No description
    #' @param est_aktiv (optional) No description
    #' @param gewst_aktiv (optional) No description
    #' @param monate (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return FristenErgebnis
    FristenApi = function(bundesland = NULL, voranmeldungsrhythmus = NULL, dauerfristverlaengerung = NULL, est_aktiv = NULL, gewst_aktiv = NULL, monate = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$FristenApiWithHttpInfo(bundesland, voranmeldungsrhythmus, dauerfristverlaengerung, est_aktiv, gewst_aktiv, monate, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param bundesland (optional) No description
    #' @param voranmeldungsrhythmus (optional) No description
    #' @param dauerfristverlaengerung (optional) No description
    #' @param est_aktiv (optional) No description
    #' @param gewst_aktiv (optional) No description
    #' @param monate (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (FristenErgebnis) with additional information such as HTTP status code, headers
    FristenApiWithHttpInfo = function(bundesland = NULL, voranmeldungsrhythmus = NULL, dauerfristverlaengerung = NULL, est_aktiv = NULL, gewst_aktiv = NULL, monate = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      query_params[["bundesland"]] <- `bundesland`

      query_params[["voranmeldungsrhythmus"]] <- `voranmeldungsrhythmus`

      query_params[["dauerfristverlaengerung"]] <- `dauerfristverlaengerung`

      query_params[["est_aktiv"]] <- `est_aktiv`

      query_params[["gewst_aktiv"]] <- `gewst_aktiv`

      query_params[["monate"]] <- `monate`

      local_var_url_path <- "/api/v1/bookkeeping/fristen"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "FristenErgebnis"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
