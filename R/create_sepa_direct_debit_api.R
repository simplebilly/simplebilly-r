#' SimpleBilly API
#'
#' Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
#'
#' The version of the OpenAPI document: 0.1.0
#' Contact: support@simplebilly.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title CreateSepaDirectDebit operations
#' @description CreateSepaDirectDebitApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  CreateSepaDirectDebitApi  ####################
#'
#' library(openapi)
#' var_creditor_name <- "creditor_name_example" # character | 
#' var_creditor_iban <- "creditor_iban_example" # character | 
#' var_creditor_id <- "creditor_id_example" # character | 
#' var_mandate_id <- "mandate_id_example" # character | 
#' var_mandate_date <- "mandate_date_example" # character | 
#' var_debtor_name <- "debtor_name_example" # character | 
#' var_debtor_iban <- "debtor_iban_example" # character | 
#' var_amount <- "amount_example" # character | 
#' var_collection_date <- "collection_date_example" # character | 
#' var_creditor_bic <- "creditor_bic_example" # character |  (Optional)
#' var_debtor_bic <- "debtor_bic_example" # character |  (Optional)
#' var_description <- "description_example" # character |  (Optional)
#'
#' api_instance <- CreateSepaDirectDebitApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateSepaDirectDebitApi(var_creditor_name, var_creditor_iban, var_creditor_id, var_mandate_id, var_mandate_date, var_debtor_name, var_debtor_iban, var_amount, var_collection_date, creditor_bic = var_creditor_bic, debtor_bic = var_debtor_bic, description = var_descriptiondata_file = "result.txt")
#' result <- api_instance$CreateSepaDirectDebitApi(var_creditor_name, var_creditor_iban, var_creditor_id, var_mandate_id, var_mandate_date, var_debtor_name, var_debtor_iban, var_amount, var_collection_date, creditor_bic = var_creditor_bic, debtor_bic = var_debtor_bic, description = var_description)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CreateSepaDirectDebitApi <- R6::R6Class(
  "CreateSepaDirectDebitApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CreateSepaDirectDebitApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' 
    #'
    #' @param creditor_name 
    #' @param creditor_iban 
    #' @param creditor_id 
    #' @param mandate_id 
    #' @param mandate_date 
    #' @param debtor_name 
    #' @param debtor_iban 
    #' @param amount 
    #' @param collection_date 
    #' @param creditor_bic (optional) No description
    #' @param debtor_bic (optional) No description
    #' @param description (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return SepaDirectDebitResponse
    CreateSepaDirectDebitApi = function(creditor_name, creditor_iban, creditor_id, mandate_id, mandate_date, debtor_name, debtor_iban, amount, collection_date, creditor_bic = NULL, debtor_bic = NULL, description = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$CreateSepaDirectDebitApiWithHttpInfo(creditor_name, creditor_iban, creditor_id, mandate_id, mandate_date, debtor_name, debtor_iban, amount, collection_date, creditor_bic, debtor_bic, description, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' 
    #'
    #' @param creditor_name 
    #' @param creditor_iban 
    #' @param creditor_id 
    #' @param mandate_id 
    #' @param mandate_date 
    #' @param debtor_name 
    #' @param debtor_iban 
    #' @param amount 
    #' @param collection_date 
    #' @param creditor_bic (optional) No description
    #' @param debtor_bic (optional) No description
    #' @param description (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (SepaDirectDebitResponse) with additional information such as HTTP status code, headers
    CreateSepaDirectDebitApiWithHttpInfo = function(creditor_name, creditor_iban, creditor_id, mandate_id, mandate_date, debtor_name, debtor_iban, amount, collection_date, creditor_bic = NULL, debtor_bic = NULL, description = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`creditor_name`)) {
        stop("Missing required parameter `creditor_name`.")
      }

      if (missing(`creditor_iban`)) {
        stop("Missing required parameter `creditor_iban`.")
      }

      if (missing(`creditor_id`)) {
        stop("Missing required parameter `creditor_id`.")
      }

      if (missing(`mandate_id`)) {
        stop("Missing required parameter `mandate_id`.")
      }

      if (missing(`mandate_date`)) {
        stop("Missing required parameter `mandate_date`.")
      }

      if (missing(`debtor_name`)) {
        stop("Missing required parameter `debtor_name`.")
      }

      if (missing(`debtor_iban`)) {
        stop("Missing required parameter `debtor_iban`.")
      }

      if (missing(`amount`)) {
        stop("Missing required parameter `amount`.")
      }

      if (missing(`collection_date`)) {
        stop("Missing required parameter `collection_date`.")
      }

      if (!missing(`creditor_name`) && is.null(`creditor_name`)) {
        stop("Invalid value for `creditor_name` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `creditor_name` is not nullable")
      }

      if (!missing(`creditor_iban`) && is.null(`creditor_iban`)) {
        stop("Invalid value for `creditor_iban` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `creditor_iban` is not nullable")
      }

      if (!missing(`creditor_id`) && is.null(`creditor_id`)) {
        stop("Invalid value for `creditor_id` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `creditor_id` is not nullable")
      }

      if (!missing(`mandate_id`) && is.null(`mandate_id`)) {
        stop("Invalid value for `mandate_id` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `mandate_id` is not nullable")
      }

      if (!missing(`mandate_date`) && is.null(`mandate_date`)) {
        stop("Invalid value for `mandate_date` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `mandate_date` is not nullable")
      }

      if (!missing(`debtor_name`) && is.null(`debtor_name`)) {
        stop("Invalid value for `debtor_name` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `debtor_name` is not nullable")
      }

      if (!missing(`debtor_iban`) && is.null(`debtor_iban`)) {
        stop("Invalid value for `debtor_iban` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `debtor_iban` is not nullable")
      }

      if (!missing(`amount`) && is.null(`amount`)) {
        stop("Invalid value for `amount` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `amount` is not nullable")
      }

      if (!missing(`collection_date`) && is.null(`collection_date`)) {
        stop("Invalid value for `collection_date` when calling CreateSepaDirectDebitApi$CreateSepaDirectDebitApi, `collection_date` is not nullable")
      }




      query_params[["creditor_name"]] <- `creditor_name`

      query_params[["creditor_iban"]] <- `creditor_iban`

      query_params[["creditor_bic"]] <- `creditor_bic`

      query_params[["creditor_id"]] <- `creditor_id`

      query_params[["mandate_id"]] <- `mandate_id`

      query_params[["mandate_date"]] <- `mandate_date`

      query_params[["debtor_name"]] <- `debtor_name`

      query_params[["debtor_iban"]] <- `debtor_iban`

      query_params[["debtor_bic"]] <- `debtor_bic`

      query_params[["amount"]] <- `amount`

      query_params[["collection_date"]] <- `collection_date`

      query_params[["description"]] <- `description`

      local_var_url_path <- "/api/v1/bookkeeping/sepa-direct-debit"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "SepaDirectDebitResponse"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
