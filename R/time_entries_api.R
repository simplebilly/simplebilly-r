#' SimpleBilly API
#'
#' Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
#'
#' The version of the OpenAPI document: 0.1.0
#' Contact: support@simplebilly.com
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title TimeEntries operations
#' @description TimeEntriesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ClockInTimeEntry  ####################
#'
#' library(openapi)
#' var_time_entry_clock_in <- TimeEntryClockIn$new("notes_example") # TimeEntryClockIn | 
#'
#' #Clock in for the authenticated user (resolved via their employee profile).
#' api_instance <- TimeEntriesApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ClockInTimeEntry(var_time_entry_clock_indata_file = "result.txt")
#' result <- api_instance$ClockInTimeEntry(var_time_entry_clock_in)
#' dput(result)
#'
#'
#' ####################  ClockOutTimeEntry  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | 
#' var_time_entry_clock_out <- TimeEntryClockOut$new("clock_out_example", "hours_example") # TimeEntryClockOut | 
#'
#' #Clock out an entry: the entry's owner, or anyone with `time_entries:write`.
#' api_instance <- TimeEntriesApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ClockOutTimeEntry(var_id, var_time_entry_clock_outdata_file = "result.txt")
#' result <- api_instance$ClockOutTimeEntry(var_id, var_time_entry_clock_out)
#' dput(result)
#'
#'
#' ####################  GetLaborCosts  ####################
#'
#' library(openapi)
#' var_from <- "from_example" # character | 
#' var_to <- "to_example" # character | 
#' var_group_by <- "group_by_example" # character | One of \"employee\", \"order\" or \"day\".
#'
#' #Labor-cost report: worked hours aggregated per employee / order / day, valued at the employee's hourly cost rate.
#' api_instance <- TimeEntriesApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetLaborCosts(var_from, var_to, var_group_bydata_file = "result.txt")
#' result <- api_instance$GetLaborCosts(var_from, var_to, var_group_by)
#' dput(result)
#'
#'
#' ####################  ListTimeEntries  ####################
#'
#' library(openapi)
#' var_from <- "from_example" # character |  (Optional)
#' var_to <- "to_example" # character |  (Optional)
#' var_active <- "active_example" # character | Only currently running shifts (clock_in set, clock_out null). (Optional)
#' var_employee_id <- "employee_id_example" # character |  (Optional)
#'
#' #List time entries with optional date-range / active / employee filters.
#' api_instance <- TimeEntriesApi$new()
#'
#' # Configure HTTP bearer authorization: bearer_token
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ListTimeEntries(from = var_from, to = var_to, active = var_active, employee_id = var_employee_iddata_file = "result.txt")
#' result <- api_instance$ListTimeEntries(from = var_from, to = var_to, active = var_active, employee_id = var_employee_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
TimeEntriesApi <- R6::R6Class(
  "TimeEntriesApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new TimeEntriesApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Clock in for the authenticated user (resolved via their employee profile).
    #'
    #' @param time_entry_clock_in 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return TimeEntryDto
    ClockInTimeEntry = function(time_entry_clock_in, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ClockInTimeEntryWithHttpInfo(time_entry_clock_in, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Clock in for the authenticated user (resolved via their employee profile).
    #'
    #' @param time_entry_clock_in 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (TimeEntryDto) with additional information such as HTTP status code, headers
    ClockInTimeEntryWithHttpInfo = function(time_entry_clock_in, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`time_entry_clock_in`)) {
        stop("Missing required parameter `time_entry_clock_in`.")
      }

      if (!missing(`time_entry_clock_in`) && is.null(`time_entry_clock_in`)) {
        stop("Invalid value for `time_entry_clock_in` when calling TimeEntriesApi$ClockInTimeEntry, `time_entry_clock_in` is not nullable")
      }

      if (!is.null(`time_entry_clock_in`)) {
        local_var_body <- `time_entry_clock_in`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/api/v1/time-entries"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TimeEntryDto"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Clock out an entry: the entry's owner, or anyone with `time_entries:write`.
    #'
    #' @param id 
    #' @param time_entry_clock_out 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return TimeEntryDto
    ClockOutTimeEntry = function(id, time_entry_clock_out, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ClockOutTimeEntryWithHttpInfo(id, time_entry_clock_out, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Clock out an entry: the entry's owner, or anyone with `time_entries:write`.
    #'
    #' @param id 
    #' @param time_entry_clock_out 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (TimeEntryDto) with additional information such as HTTP status code, headers
    ClockOutTimeEntryWithHttpInfo = function(id, time_entry_clock_out, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`time_entry_clock_out`)) {
        stop("Missing required parameter `time_entry_clock_out`.")
      }

      if (!missing(`id`) && is.null(`id`)) {
        stop("Invalid value for `id` when calling TimeEntriesApi$ClockOutTimeEntry, `id` is not nullable")
      }

      if (!missing(`time_entry_clock_out`) && is.null(`time_entry_clock_out`)) {
        stop("Invalid value for `time_entry_clock_out` when calling TimeEntriesApi$ClockOutTimeEntry, `time_entry_clock_out` is not nullable")
      }

      if (!is.null(`time_entry_clock_out`)) {
        local_var_body <- `time_entry_clock_out`$toJSONString()
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/api/v1/time-entries/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "TimeEntryDto"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Labor-cost report: worked hours aggregated per employee / order / day, valued at the employee's hourly cost rate.
    #'
    #' @param from 
    #' @param to 
    #' @param group_by One of \"employee\", \"order\" or \"day\".
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[LaborCostRow]
    GetLaborCosts = function(from, to, group_by, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$GetLaborCostsWithHttpInfo(from, to, group_by, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Labor-cost report: worked hours aggregated per employee / order / day, valued at the employee's hourly cost rate.
    #'
    #' @param from 
    #' @param to 
    #' @param group_by One of \"employee\", \"order\" or \"day\".
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[LaborCostRow]) with additional information such as HTTP status code, headers
    GetLaborCostsWithHttpInfo = function(from, to, group_by, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`from`)) {
        stop("Missing required parameter `from`.")
      }

      if (missing(`to`)) {
        stop("Missing required parameter `to`.")
      }

      if (missing(`group_by`)) {
        stop("Missing required parameter `group_by`.")
      }

      if (!missing(`from`) && is.null(`from`)) {
        stop("Invalid value for `from` when calling TimeEntriesApi$GetLaborCosts, `from` is not nullable")
      }

      if (!missing(`to`) && is.null(`to`)) {
        stop("Invalid value for `to` when calling TimeEntriesApi$GetLaborCosts, `to` is not nullable")
      }

      if (!missing(`group_by`) && is.null(`group_by`)) {
        stop("Invalid value for `group_by` when calling TimeEntriesApi$GetLaborCosts, `group_by` is not nullable")
      }

      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      query_params[["group_by"]] <- `group_by`

      local_var_url_path <- "/api/v1/labor-costs"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[LaborCostRow]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List time entries with optional date-range / active / employee filters.
    #'
    #' @param from (optional) No description
    #' @param to (optional) No description
    #' @param active (optional) Only currently running shifts (clock_in set, clock_out null).
    #' @param employee_id (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return array[TimeEntryDto]
    ListTimeEntries = function(from = NULL, to = NULL, active = NULL, employee_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      local_var_response <- self$ListTimeEntriesWithHttpInfo(from, to, active, employee_id, data_file = data_file, ..., .parse = .parse)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List time entries with optional date-range / active / employee filters.
    #'
    #' @param from (optional) No description
    #' @param to (optional) No description
    #' @param active (optional) Only currently running shifts (clock_in set, clock_out null).
    #' @param employee_id (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @param .parse Logical. If \code{TRUE} then the response will be parsed to a generated type. If \code{FALSE} the response will be returned as unparsed text.
    #'
    #' @return API response (array[TimeEntryDto]) with additional information such as HTTP status code, headers
    ListTimeEntriesWithHttpInfo = function(from = NULL, to = NULL, active = NULL, employee_id = NULL, data_file = NULL, ..., .parse = TRUE) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      query_params[["from"]] <- `from`

      query_params[["to"]] <- `to`

      query_params[["active"]] <- `active`

      query_params[["employee_id"]] <- `employee_id`

      local_var_url_path <- "/api/v1/time-entries"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          self$api_client$WriteFile(local_var_resp, data_file)
        }
        if (!.parse) {
          local_var_resp$content <- local_var_resp$response_as_text()
          return(local_var_resp)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$DeserializeResponse(local_var_resp, "array[TimeEntryDto]"),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(content = paste("Server returned ", local_var_resp$status_code, " response status code."),
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new(content = "API client error",
                        response = local_var_resp,
                        status_code = local_var_resp$status_code)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || all(local_var_resp$response == "")) {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
